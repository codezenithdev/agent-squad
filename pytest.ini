[pytest]
# asyncio_mode=auto lets us write `async def test_...` without a decorator.
asyncio_mode = auto
testpaths = tests
# Put the project root on sys.path so tests can `from config import ...`, etc.
pythonpath = .
